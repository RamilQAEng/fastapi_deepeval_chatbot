# OpenAI / OpenRouter Configuration
# We use OpenRouter as a proxy for OpenAI-compatible APIs
OPENAI_API_KEY=sk-or-your-openrouter-key-here
OPENAI_API_BASE=https://openrouter.ai/api/v1

# DeepEval Configuration
# DeepEval uses langchain/openai under the hood. 
# We configure it to use OpenRouter by setting the model name and base URL.
DEEPEVAL_OPENAI_MODEL=google/gemini-2.0-flash-exp:free
# If DeepEval requires a specific env var for the key different from OPENAI_API_KEY, set it here.
# Typically it respects standard OpenAI env vars.

# Database Configuration (Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=pet_project_rag

# Application Settings
LOG_LEVEL=INFO
